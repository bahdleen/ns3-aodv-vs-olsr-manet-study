# Archived Helpers

This folder contains earlier helper scripts that were created during development but are not part of the final published workflow.

Some of them refer to an older `mesh-metrics` experiment shape rather than the final `scratch/manet_compare.cc` scenario. They are kept only for historical reference and should not be treated as the main entry points for reproducing the results in this repository.
